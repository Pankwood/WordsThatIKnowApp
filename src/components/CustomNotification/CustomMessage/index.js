const errorCheckWordsPost = "Error to save words in your dictionary";

const errorCheckWordsGet = "Error to get your known words";

const errorEmptyValues = "You must choose at least one word";

const successCheckWordsPost = "Word(s) saved!";


export { errorCheckWordsPost, errorCheckWordsGet, errorEmptyValues, successCheckWordsPost }